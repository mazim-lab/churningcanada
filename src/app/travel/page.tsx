import { BoardingSoon } from "@/components/BoardingSoon";

export const metadata = { title: "Travel & Points — FinTerminal" };

export default function TravelPage() {
  return <BoardingSoon title="Travel & Points" />;
}
